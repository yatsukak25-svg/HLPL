@model IReadOnlyList<StudentDto>
@{
    ViewData["Title"] = "Учні";
}

<div class="d-flex justify-content-between align-items-center mb-3">
    <h1 class="h3 mb-0">Учні</h1>
    <a class="btn btn-primary" asp-action="Create">Додати учня</a>
</div>

<div class="card border-0 shadow-sm">
    <div class="card-body">
        <table class="table align-middle">
            <thead><tr><th>ПІБ</th><th>Email</th><th>Телефон</th><th>Клас</th><th></th></tr></thead>
            <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>@item.FullName</td>
                    <td>@item.Email</td>
                    <td>@item.PhoneNumber</td>
                    <td>@item.Grade</td>
                    <td class="text-end">
                        <a class="btn btn-sm btn-outline-secondary" asp-action="Edit" asp-route-id="@item.Id">Редагувати</a>
                        <a class="btn btn-sm btn-outline-danger" asp-action="Delete" asp-route-id="@item.Id">Видалити</a>
                    </td>
                </tr>
            }
            </tbody>
        </table>
    </div>
</div>
